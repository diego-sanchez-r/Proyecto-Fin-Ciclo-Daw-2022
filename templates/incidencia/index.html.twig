{% extends 'base.html.twig' %}

{% block title %}Hello IncidenciaController!{% endblock %}

{% block body %}
<style>
    .example-wrapper { margin: 1em auto; max-width: 800px; width: 95%; font: 18px/1.5 sans-serif; }
    .example-wrapper code { background: #F5F5F5; padding: 2px 6px; }
</style>

<div class="example-wrapper">
    <h1>Hello {{ controller_name }}! ✅</h1>        
    {% for incidencia in mis_incidencias %}
        Incidencia: <a href="{{ path("ver_incidencia",{id:incidencia.id}) }}">{{ incidencia.descripcion }}</a>
        <p>{{incidencia.descripcion}}</p>
        <p>{{incidencia.averia.nombre}}</p>
        <p>Comentarios</p>
        {% for comentario in incidencia.comentarios %}
            <p>{{comentario.texto}}</p>
        {%  endfor %}
    {%  endfor %}
</div>
{% endblock %}
